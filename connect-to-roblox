--// Variables
local scriptUrls = {
    [6701277882] = "/Getkey",
    [121864768012064] = "/Getkey"
}

local scriptUrl = scriptUrls[game.PlaceId]
if scriptUrl then
    local success, errorMsg = pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Wwwaaallvvvyyy666/Fishit/refs/heads/main" .. scriptUrl))()
    end)
    if not success then
        warn("[Walvy Community] Failed to load script:", errorMsg)
    end
else
    print("[Walvy Community] Failed to load script Fish It.")
end
